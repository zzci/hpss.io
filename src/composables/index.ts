export const baseUrl = window.location.origin;

// Authentication related configurations
export const APPLICATION_BASE_URL = window.location.origin;
export const AUTH_CALLBACK_URL = `${APPLICATION_BASE_URL}/callback`;
export const SINGLE_SIGN_ON_LOGIN_URL = `${APPLICATION_BASE_URL}/user/ssoLogin`;

// Logto identity service configurations
export const LOGTO_APPLICATION_ID = LOGTO_APP_ID;
export const LOGTO_ENDPOINT_URL = LOGTO_ENDPOINT;
